{
  "name": "@devly/devly-apache",
  "version": "2.2.1",
  "description": "Apache plugin for `@devly` cli manager built with redux and yargs.",
  "main": "index.js",
  "scripts": {
    "version": "npm run build && git add .",
    "postversion": "git push && git push --tags",
    "test": "echo \"Error: no test specified\" && exit 0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/devlyjs/devly-apache"
  },
  "keywords": [
    "devly",
    "@devly",
    "apache"
  ],
  "author": "Aori Nevo",
  "license": "ISC",
  "devDependencies": {
    "@devly/devly-store": "^1.1.0",
    "shelljs": "^0.8.2",
    "winston": "^2.4.4"
  },
  "dependencies": {}
}
